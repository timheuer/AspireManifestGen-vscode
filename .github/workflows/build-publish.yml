name: Build and Publish

on:
    workflow_dispatch:
    push:
        branches:
            - main
        paths-ignore:
            - "**/*.md"
            - "**/*.gitignore"
            - "**/*.gitattributes"

jobs:
    build:
        permissions:
            contents: write
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4
              with:
                  fetch-depth: 0

            - uses: actions/setup-node@v4
              with:
                  node-version: 20

            - name: Read extension metadata
              id: extension
              shell: bash
              run: |
                  echo "marketplace_id=$(node -p \"const pkg = require('./package.json'); pkg.publisher + '.' + pkg.name\")" >> "$GITHUB_OUTPUT"
                  echo "vsix_name=${{ github.event.repository.name }}.vsix" >> "$GITHUB_OUTPUT"

            - name: Install dependencies
              run: npm install

            - name: Install vsce
              run: npm install -g @vscode/vsce

            - name: NBGV
              uses: dotnet/nbgv@master
              id: nbgv
              with:
                  stamp: package.json

            - name: NBGV outputs
              run: |
                  echo "SimpleVersion: ${{ steps.nbgv.outputs.SimpleVersion }}"

            - name: Package
              run: |
                  vsce package -o "./${{ steps.extension.outputs.vsix_name }}"

            - name: Publish
              if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
              run: npm run deploy
              env:
                  VSCE_PAT: ${{ secrets.VSCE_PAT }}

            - name: Marketplace Link
              if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
              shell: pwsh
              run: |
                  Write-Host "::notice::Published to VS Code Marketplace: https://marketplace.visualstudio.com/items?itemName=${{ steps.extension.outputs.marketplace_id }}"

            - name: Upload artifact
              uses: actions/upload-artifact@v4
              with:
                  name: ${{ steps.extension.outputs.vsix_name }}
                  path: ./*.vsix

            - name: Tag and Release
              if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
              uses: softprops/action-gh-release@v1
              with:
                  body: Release ${{ steps.nbgv.outputs.SimpleVersion }}
                  tag_name: ${{ steps.nbgv.outputs.SimpleVersion }}
                  generate_release_notes: true
                  files: ./*.vsix
